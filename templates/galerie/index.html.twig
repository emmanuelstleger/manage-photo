{% extends 'base-admin.html.twig' %}

{% block title %}Galerie index{% endblock %}

{% block body %}
    <h1>Index des galeries</h1>
    <a href="{{ path('galerie_new') }}" class="btn btn-success btn-lg mb-3">Créer une galerie</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        {% for galerie in galeries %}
            <tr>
                <td>{{ galerie.id }}</td>
                <td>{{ galerie.titre }}</td>
                <td>{{ galerie.description }}</td>
                <td>
                    <a href="{{ path('galerie_edit', {'id': galerie.id}) }}" class="btn btn-success btn-sm"> <i class="fa fa-pencil-alt"></i> Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
