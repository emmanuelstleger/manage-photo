{{ form_start(form) }}
    <div class="form-group">
        <label for="galleryTitle">Titre</label>
        {{ form_widget(form.titre, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        <label for="galleryDescription">Description</label>
        {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="form-group">
        <label for="galleryImage">Image à la une</label>
        {{ form_widget(form.imageFile, {'attr': {'class': 'form-control'}}) }}
    </div>

    <button class="btn btn-success">
        <i class="fa fa-edit"></i>
        {{ button_label|default('Sauvegarder') }}</button>
    <a class="btn btn-primary" href="{{ path('galerie_admin_index') }}">Retour</a>
{{ form_end(form) }}
