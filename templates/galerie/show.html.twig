{% extends 'base-admin.html.twig' %}

{% block title %}Galerie{% endblock %}

{% block body %}
    <h1>Galerie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Image à la une</th>
                <td><img src="{{ vich_uploader_asset(galerie) }}" alt="" style="width: 100px; height: 100px;"></td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ galerie.id }}</td>
            </tr>

            <tr>
                <th>Titre</th>
                <td>{{ galerie.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ galerie.description }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('galerie/_delete_form.html.twig') }}
    <a href="{{ path('galerie_admin_index') }}" class="btn btn-primary btn-sm d-inline-block">Retour</a>

{% endblock %}
